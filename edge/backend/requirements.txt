# NexaSecure AI — edge backend dependencies (Phase 0/1).
# Pinned loosely for a beginner-friendly setup; tighten before production.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
SQLAlchemy>=2.0,<3.0
python-jose[cryptography]>=3.3,<4.0   # JWT (auth, used from Phase 0 onward)
passlib[bcrypt]>=1.7,<2.0             # password hashing (auth)

# ---- dev / test ----
pytest>=8.0,<9.0
httpx>=0.27,<1.0                       # test client for FastAPI
